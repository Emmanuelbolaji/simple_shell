#include <stdio.h>
#include <stdlib.h>

/**
 * main - entry point
 * Return: 0 on success
 */

int main(void)
{
	int x = 56;
	int y = 65;

	int sum = x + y;

	printf("sum = %d\n", sum);

	return (0);
}
